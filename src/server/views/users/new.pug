extends /layouts/application.pug

block content
  +formFor(f, { url: urlFor('registerUser'), method: 'post' })
    +input(f, 'email')
    +input(f, 'firstName')
    +input(f, 'lastName')
    +input(f, 'password', { as: 'password' })
    +button(f, 'Register')
