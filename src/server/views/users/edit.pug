extends /layouts/application.pug

block content
  +formFor(f, { url: urlFor('saveCurrentUserProfile'), method: 'patch' })
    +input(f, 'email')
    +input(f, 'firstName')
    +input(f, 'lastName')
    +button(f)
